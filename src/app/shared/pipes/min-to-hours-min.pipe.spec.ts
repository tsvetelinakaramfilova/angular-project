import { MinToHoursMinPipe } from './min-to-hours-min.pipe';

describe('MinToHoursMinPipe', () => {
  it('create an instance', () => {
    const pipe = new MinToHoursMinPipe();
    expect(pipe).toBeTruthy();
  });
});
